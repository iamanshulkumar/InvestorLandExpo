import React from 'react';
import {ActivityIndicator} from 'react-native';

export const MtActivityIndicator = props => {
  return (<>
      <ActivityIndicator color="#0000ff" size="large" style={{marginTop:50}} />
    </>);
};